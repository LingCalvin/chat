import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
  root: {
    textAlign: 'center',
  },
  idSkeleton: {
    width: '36ch',
    margin: 'auto',
  },
}));

export default useStyles;
